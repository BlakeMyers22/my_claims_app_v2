{
  "name": "claims-assistant",
  "version": "1.0.0",
  "description": "AI-powered forensic engineering report generator with feedback storing and automatic fine-tuning in GitHub Actions",
  "main": "index.js",
  "scripts": {
    "build": "echo \"No build step needed\" && exit 0",
    "test": "echo \"No tests\" && exit 0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.10.0",
    "axios": "^1.6.2",
    "openai": "^4.0.0"
  }
}
